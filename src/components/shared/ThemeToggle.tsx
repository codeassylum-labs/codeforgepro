    py-2 rounded-md shadow-md hover:bg-[#E0E0E0] transition"
      onClick={toggleTheme}
    >
      Toggle to {theme === 'light' ? 'Dark' : 'Light'} Mode
    </button>
  );
};

export default ThemeToggle;
    py-2 rounded-md shadow-md hover:bg-[#E0E0E0] transition"
      onClick={toggleTheme}
    >
      Toggle to {theme === 'light' ? 'Dark' : 'Light'} Mode
    </button>
  );
};

export default ThemeToggle;
    py-2 rounded-md shadow-md hover:bg-[#E0E0E0] transition"
      onClick={toggleTheme}
    >
      Toggle to {theme === 'light' ? 'Dark' : 'Light'} Mode
    </button>
  );
};

export default ThemeToggle;
    py-2 rounded-md shadow-md hover:bg-[#E0E0E0] transition"
      onClick={toggleTheme}
    >
      Toggle to {theme === 'light' ? 'Dark' : 'Light'} Mode
    </button>
  );
};

export default ThemeToggle;
